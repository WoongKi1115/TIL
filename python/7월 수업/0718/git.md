로컬에서 작업.

working directory 

staging area
메모리 상에서 관리
repository


커밋할 파일들을 staging area에 먼저 올려둔 후
커밋을 통해 repository에 저장됨.

touch 명령어로 파일 생성
cd 를 통해 위치 변경 가능

vscode에서 위치를 알려줌
u => working directory
A => staging area

문서 수정을 한뒤에 
git add
git commit -m'' 해줌

그럼 커밋 되어있음.